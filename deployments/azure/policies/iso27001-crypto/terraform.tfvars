# Enforcement Mode
# "Default" = Policies are enforced (deny/audit)
# "DoNotEnforce" = Audit-only mode (recommended for initial deployment)
enforcement_mode = "DoNotEnforce"

# Optional: Specify subscription ID
# If not provided, uses current subscription
# subscription_id = "your-subscription-id"
